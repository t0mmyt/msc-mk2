#!/usr/bin/env python3
from distutils.core import setup

setup(
    name='obsloader',
    version='1.0',
    author='Tom Taylor',
    description=
        'Converts raw SAC files to a tsdatastore payload and uploads them',
    packages=['obsloader']
)
